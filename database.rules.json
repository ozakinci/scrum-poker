{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "players": {
      "$playerId": {
        ".write": "(!data.exists() && newData.child('ownerUid').val() === auth.uid) || (data.exists() && data.child('ownerUid').val() === auth.uid)"
      }
    },
    "state": {
      ".write": "auth != null"
    }
  }
}
